# DevOps evaluation prompts

Same workflow. The bar here is "I'd accept this PR". Manifests are
correct, side effects are explicit, secrets are not invented.

**Target model (v0.3.0):** `qwen3-27b` (Modelfile alias for
`qwen3.8:27b`). Same persona and sampling defaults as the Go and C#
suites; see `tests/prompts/go/README.md` for the variant choice
guidance.

## Known regressions

(none yet)